/**
 * 延迟时间常量（毫秒）
 */
export const DELAY = {
  /** 侧边栏收起延迟 */
  SIDEBAR_COLLAPSE: 250,
  /** 默认防抖延迟 */
  DEBOUNCE_DEFAULT: 450,
  /** 弹窗动画延迟 */
  POPUP_ANIMATION: 220,
  /** 默认节流延迟 */
  THROTTLE_DEFAULT: 300,
};

/**
 * 尺寸常量（像素）
 */
export const SIZE = {
  /** 侧边栏宽度 */
  SIDEBAR_WIDTH: 240,
  /** 侧边栏收起时宽度 */
  SIDEBAR_COLLAPSED_WIDTH: 0,
};

/**
 * 编辑器相关常量
 */
export const EDITOR = {
  /** 内容预览截取长度 */
  EXCERPT_LENGTH: 180,
};

/**
 * 文档相关常量
 */
export const DOCUMENT = {
  /** 文档内容最大字符数 */
  MAX_DOCUMENT_SIZE: 100000,
  /** 显示警告的阈值（0-1） */
  WARNING_THRESHOLD: 0.8,
};

/**
 * 动画常量
 */
export const ANIMATION = {
  /** 弹簧弹跳动画时长（秒） */
  SPRING_BOUNCE: 0.5,
  /** 弹簧平滑动画时长（秒） */
  SPRING_SMOOTH: 0.3,
};
